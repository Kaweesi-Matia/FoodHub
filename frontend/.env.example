# Local Vite proxy is used automatically in production builds if this
# still points at localhost. For a split Vercel + Render deploy you can
# either omit this or set it to your Render API URL including /api:
# VITE_API_URL=https://foodhub-4etq.onrender.com/api
VITE_API_URL=http://localhost:5000/api
